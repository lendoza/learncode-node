var assert = require("assert");
var math = require("./math");

// Assert evaluates something to be true - else error

assert(math.add(3, 4) === 7);

assert(math.subtract(3, 4) === -1);

assert(math.multiply(3, 4) === 11);

